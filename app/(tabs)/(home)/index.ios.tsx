
// iOS-specific home screen
// Currently uses the same implementation as the base index.tsx
// This file exists to allow for future iOS-specific customizations

import HomeScreen from './index';

export default HomeScreen;
